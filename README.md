# cnn-face-recognition-embeddings-knn-openset
CNN do zero para gerar embeddings faciais (128D) + PCA + KNN com lógica de “Unknown” via threshold de distância (open-set). Dataset privado com consentimento (não publicado).
